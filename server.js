const express = require('express');

const app = express();

function createProfile() {
  app.post('/', (req, res) => {
    
  });
);

app.listen(3000, () => {
  console.log('listening on 3000');
});
