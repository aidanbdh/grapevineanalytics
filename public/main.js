const createProfileForm = document.getElementById('create-profile');

function NewProfile(first, last, username, password, email) {
  this.firstName = first;
  this.lastName = last;
  this.username = username;
  this.password = password;
  this.email = email;
}

createProfileForm.addEventListener(submit, target => {
  
}, true);
